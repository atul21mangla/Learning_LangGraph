[project]
name = "langgraph-chatbot-ui"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.56.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["backend*", "frontend*"]